import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "OHP - Mobile-First Next.js Application",
  description: "A modern Next.js 14 application built with TypeScript, Tailwind CSS, and shadcn/ui, designed with a mobile-first approach for optimal user experience across all devices.",
  keywords: ["Next.js", "TypeScript", "Tailwind CSS", "shadcn/ui", "Mobile-First", "React"],
  authors: [{ name: "OHP Team" }],
  viewport: "width=device-width, initial-scale=1",
  robots: "index, follow",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
